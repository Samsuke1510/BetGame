{
  "name": "betgame",
  "version": "1.0.0",
  "private": true,
  "description": "A virtual-money MLB sports betting app (web + backend). No real money involved.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w server && npm run build -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
